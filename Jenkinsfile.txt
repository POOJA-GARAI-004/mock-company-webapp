pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Commands to build the project (e.g., using Gradle, Maven, etc.)
                sh './gradlew clean build'
            }
        }
        
        stage('Test') {
            steps {
                // Commands to run tests (e.g., unit tests, integration tests)
                sh './gradlew test'
            }
        }
        
        stage('Deploy') {
            steps {
                // Commands to deploy the application (e.g., to a test environment)
                sh './gradlew deploy'
            }
        }
    }
    
    post {
        success {
            // Actions to take if the pipeline succeeds (e.g., notify teams)
            echo 'Pipeline succeeded! Deploy to production.'
        }
        failure {
            // Actions to take if the pipeline fails (e.g., send notifications)
            echo 'Pipeline failed! Notify developers.'
        }
    }
}
